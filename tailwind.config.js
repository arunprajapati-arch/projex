// Note: This assumes you already have a tailwind.config.js. 
// If you do, just add the scrollbar plugin to the existing configuration.

module.exports = {
  // ...existing tailwind config...
  plugins: [
    // ...existing plugins without the scrollbar plugin...
  ],
}
